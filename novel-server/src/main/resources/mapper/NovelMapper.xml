<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wang.novel.mapper.NovelMapper">

    <!-- 分页查询热门小说（支持分类筛选） -->
    <select id="selectHotNovelsByPage" resultType="com.wang.pojo.entity.Novel">
        SELECT DISTINCT n.*
        FROM novel n
        LEFT JOIN novel_category_relation ncr ON n.id= ncr.novel_id
        WHERE n.is_hot = 1
        AND n.if_del = 0
        <if test="categoryId != null">
            AND ncr.category_id= #{categoryId}
        </if>
        ORDER BY n.update_time DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <!-- 统计热门小说总数（支持分类筛选） -->
    <select id="countHotNovels" resultType="java.lang.Integer">
        SELECT COUNT(DISTINCT n.id)
        FROM novel n
        LEFT JOIN novel_category_relation ncr ON n.id = ncr.novel_id
        WHERE n.is_hot = 1
        AND n.if_del = 0
        <if test="categoryId != null">
            AND ncr.category_id = #{categoryId}
        </if>
    </select>

    <!-- 查询小说详情（包含统计信息） -->
    <select id="selectNovelDetail" resultType="java.util.Map">
        SELECT n.*,
               (SELECT COUNT(*) FROM novel_character nc WHERE nc.novel_id= n.id) as character_count,
               (SELECT COUNT(*) FROM faction f WHERE f.novel_id = n.id) as faction_count,
               (SELECT COUNT(*) FROM location l WHERE l.novel_id = n.id) as location_count
        FROM novel n
        WHERE n.id = #{novelId}
        AND n.if_del = 0
    </select>

    <!-- 根据分类 ID 分页查询小说 -->
    <select id="selectNovelsByCategoryId" resultType="com.wang.pojo.entity.Novel">
        SELECT n.*
        FROM novel n
        INNER JOIN novel_category_relation ncr ON n.id = ncr.novel_id
        WHERE ncr.category_id = #{categoryId}
        AND n.if_del = 0
        ORDER BY n.update_time DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <!-- 统计分类下小说总数 -->
    <select id="countNovelsByCategoryId" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM novel n
        INNER JOIN novel_category_relation ncr ON n.id= ncr.novel_id
        WHERE ncr.category_id = #{categoryId}
        AND n.if_del = 0
    </select>

</mapper>